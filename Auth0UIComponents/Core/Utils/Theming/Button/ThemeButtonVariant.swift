public enum ThemeButtonVariant {
    case primary   // Solid background (uses theme.primary)
    case outline   // Border only (uses theme.border)
    case ghost     // No background/border (uses theme.textPrimary)
}
